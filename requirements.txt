# ===== Основные зависимости проекта =====
# Behave — BDD-фреймворк для тестов
behave==1.2.6

# Selenium — управление браузером (локально и через BrowserStack)
# 4.36.0 — последняя версия с поддержкой Python 3.9
# Обновлено с 4.10.0 — исправляет баг 'dict' object в Remote WebDriver
selenium==4.36.0

# ===== Остальные зависимости (pip установит автоматически) =====
# Зависимости selenium и behave разрешатся автоматически при установке.
# Ниже — дополнительные пакеты, нужные для тестов:
colorama==0.4.6
pytest==7.4.3
